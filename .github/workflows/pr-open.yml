name: PR Deployment

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'docs/**'
      - '.github/workflows/pr-open.yml'
      - 'tests/**'
      - 'package*.json'
      - 'playwright.config.js'

# Workflow-level: per-PR cancellation so rapid pushes don't queue stale runs.
# The deploy job adds its own gh-pages-state lock to serialize gh-pages mutations.
concurrency:
  group: pr-preview-${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  validate:
    name: Lint & Unit Tests
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Verify syntax, formatting & run unit tests
        run: npm run validate

  e2e:
    name: E2E Playwright Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Get Playwright version
        id: playwright-version
        run: |
          echo "version=$(node -e 'console.log(require("@playwright/test/package.json").version)')" >> $GITHUB_OUTPUT
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}
          restore-keys: |
            ${{ runner.os }}-playwright-
      - name: Install Playwright Chromium & dependencies
        run: |
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
            npx playwright install-deps chromium
          else
            npx playwright install --with-deps chromium
          fi
      - name: Compile JSON to FlatGeobuf (e2e species only)
        run: SPECIES_ONLY=Pli,Fdi,Fdc,Sx,Yc npm run db:convert
      - name: Run E2E Integration tests
        run: npm run test:e2e

  deploy:
    name: Deploy PR Preview
    needs: [validate, e2e]
    if: github.event.pull_request.head.repo.full_name == github.repository
    # Serialize gh-pages mutations across pr-open, pr-close, and merge workflows.
    concurrency:
      group: gh-pages-state
      cancel-in-progress: false
    permissions:
      contents: write
      pages: write
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Compile full database for preview
        run: |
          # ---------------------------------------------------------------------------
          # TEMPORARY — FORCE_FULL_FGB_DEPLOY (remove after ESRI stack merges to main)
          # ---------------------------------------------------------------------------
          # While the flatgeobuf / ESRI-eviction PR stack is open, PR previews must ship
          # the full compiled database locally. The normal "pruned" path only keeps polygon
          # outlines and expects seedlot FGB from production root via ../../ — but production
          # gh-pages does not yet run db:convert. Documented in PR #99.
          # ---------------------------------------------------------------------------
          echo "📂 Full database deploy: compiling JSON to FlatGeobuf."
          npm run db:convert

          # Drop compiled-source JSON from the deploy. The app fetches the .fgb
          # equivalents at runtime (fetchFGB rewrites *_Seedlots.json /
          # *_migrated_height_list_*.json to .fgb), so these ~500MB of source
          # files are dead weight that bloat gh-pages and break the Pages build.
          # Orchard_list.json, Seedlot_list.json and bec_bounds.json ARE fetched
          # as JSON at runtime and must stay.
          find docs/Version_7_0 -type f \( -name '*_Seedlots.json' -o -name '*_migrated_height_list_*.json' \) -delete

          sed -i 's/<body/<body data-full-fgb-deploy="true"/' docs/index.html
      - name: Deploy to deployments/pr-${{ github.event.number }}
        uses: JamesIves/github-pages-deploy-action@d92aa235d04922e8f08b40ce78cc5442fcfbfa2f # v4
        with:
          folder: docs
          branch: gh-pages
          clean: true # Removes stale files from the target subfolder only — safe because
          # target-folder scopes rsync's --delete to deployments/pr-{N}/, not other PR folders.
          target-folder: deployments/pr-${{ github.event.number }}

      - name: Verify full preview on gh-pages branch
        run: |
          git fetch origin gh-pages
          PREVIEW="deployments/pr-${{ github.event.number }}"
          FGB_COUNT=$(git ls-tree --name-only origin/gh-pages "${PREVIEW}/Version_7_0/" | grep -c '\.fgb$' || true)
          if [ "$FGB_COUNT" -lt 80 ]; then
            echo "❌ Expected full FGB deploy on gh-pages, found ${FGB_COUNT} .fgb files"
            exit 1
          fi
          git cat-file -e "origin/gh-pages:${PREVIEW}/Version_7_0/Pli_Seedlots.fgb"
          git show "origin/gh-pages:${PREVIEW}/index.html" | grep -q 'data-full-fgb-deploy="true"' \
            || { echo "❌ index.html missing data-full-fgb-deploy"; exit 1; }
          echo "✅ gh-pages has full preview (${FGB_COUNT} .fgb files)"

      - name: Log CDN status (informational)
        continue-on-error: true
        run: |
          PREVIEW_BASE="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/deployments/pr-${{ github.event.number }}"
          INDEX=$(curl -o /dev/null -s -w "%{http_code}" "$PREVIEW_BASE/" || true)
          FGB=$(curl -o /dev/null -s -w "%{http_code}" "$PREVIEW_BASE/Version_7_0/Pli_Seedlots.fgb" || true)
          echo "CDN: index=${INDEX} Pli_Seedlots.fgb=${FGB} at ${PREVIEW_BASE}/"
          if [ "$INDEX" = "200" ] && [ "$FGB" = "200" ]; then
            echo "✅ CDN is serving the full preview."
          else
            echo "::warning::GitHub Pages CDN may lag behind gh-pages (index=${INDEX}, fgb=${FGB}). Use npm run dev locally; re-test preview after CDN catches up."
          fi
